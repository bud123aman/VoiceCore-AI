# Core
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
httpx==0.27.2
pydantic==2.10.6
numpy==1.26.4
requests==2.32.3

# Q2 — Knowledge base
qdrant-client==1.11.1
sentence-transformers==3.1.1
rank-bm25==0.2.2

# Q1 + Q3 — Voice agent
# SmallWebRTCTransport (no external service, no account needed)
# Install with: pip install "pipecat-ai[webrtc,silero,google]"
pipecat-ai==1.5.0
pipecat-ai-small-webrtc-prebuilt>=0.0.8   # prebuilt browser UI for SmallWebRTC

# Q4 — Local ASR (free, no API needed)
faster-whisper==1.1.0

# LLM — Gemini (free tier, 1500 req/day)
google-genai>=0.8.0
